User {{ user.username }} tried to delete the following object:

{{ instance.HandleRef.tag }}-{{ instance.id }}: {{ instance }}

The deletion was prevented because of the following reason(s):

{{ instance.not_deletable_reason }}

{{ admin_url }}
